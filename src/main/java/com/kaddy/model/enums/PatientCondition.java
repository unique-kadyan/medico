package com.kaddy.model.enums;

public enum PatientCondition {
    CRITICAL, // Requires immediate attention
    SERIOUS, // Serious condition, needs close monitoring
    STABLE, // Condition is stable
    IMPROVING, // Patient condition is improving
    DISCHARGED // Patient has been discharged from ER
}
